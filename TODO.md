# TODO

## Core

- Verify that users can only access their own decks and cards.
- Test the full auth flow: register, login, me, logout.

## UX

- Add better error messages for register and login.
- Add loading and disabled states for login, create deck, edit deck, create card, edit card, and delete actions.
- Add simple frontend validation for required fields.

## Stability

- Handle invalid or empty API responses more safely.
- Run through all app flows manually and fix remaining edge-case bugs.

## Finish

- Do a final cleanup pass on code and UI text.
- Update `README.md` if setup or usage changed.
